#include <iostream>
using namespace std;

int main() {
	//code
	int t,n,a,b,c,count;
	cin>>t;
	while(t--)
	{
	    c=0,count=0;
	    cin>>n;
	    for(int i=1;i<=n;i++)
	    {
	        a=i*i*i;
	        for(int j=0;j<n;j++)
	        {
	            b=j*j*j;
	            c=a+b;
	            if(c==n)
	            {
	                count++;
	            }
	        }
	    }
	    cout<<count<<endl;
	}
	return 0;
}
////////////////////////////////////////////////////////////////

Given N, count all ‘a’(>=1) and ‘b’(>=0) that satisfy the condition a3 + b3 = N.


////////////////////////////////////////////////////////////////
